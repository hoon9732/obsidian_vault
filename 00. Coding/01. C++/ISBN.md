```
#include <iostream>
#incluide <string>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	string s;
	cin >> s;
	int sum = 0;

	for (int i = 0; i < 13; ++i) {
		if (s[i] == )
	}


}

```
#Code #Encapsulation 